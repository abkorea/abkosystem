# ABKOSYSTEM Desktop

This is a minimal Electron scaffold for packaging Windows artifacts.

## Local run
```bash
cd desktop-app
npm install
npm run start
```

## Build
```bash
cd desktop-app
npm ci
npm run dist
```

